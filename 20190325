The template of class

类模版：
　　　　template<class  形参名，class 形参名，…>   class 类名

方法模版：
　　　　template <class 形参名，class 形参名，......> 返回类型 函数名(参数列表)

两种模版都可以随便使用。

template<class T>
bool tpl(T a, T b) {
    return a > b;
};

    tpl(22, 33);
    tpl(aa, bb);
    tpl('a', 'b');
    tpl(2.2, 3.3);
    tpl("aaaa", "bbbb");
    
编译都不报任何都错误。

算法：
确定星座。
先创建一个2维都数组，{{狮子座，白羊座}，{白羊座，处女座}... ...}这样的数组。
然后创建一个1维都数组。{21，22，25，20，19... ...}。
最后再用输入来确定出输出。
